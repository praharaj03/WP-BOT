# AI provider keys (keep the real values only in your local .env)
GROQ_API_KEY=YOUR_GROQ_API_KEY
GROQ_MODEL=openai/gpt-oss-20b

GEMINI_API_KEY=YOUR_GEMINI_API_KEY
GEMINI_MODEL=gemini-3.6-flash

PORT=3000
REPLY_DELAY_MS=4000

# Local-only setup: no public tunnel or CONTROL_TOKEN is required.
